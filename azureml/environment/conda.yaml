name: bert-finetune
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.0.0
      - torchvision
      - torchaudio
      - transformers>=4.30.0
      - peft>=0.4.0
      - tqdm
      - datasets
      - scikit-learn
      - numpy
      - pandas

